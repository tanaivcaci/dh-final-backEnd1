spring:
  application:
    name: api-gateway
  cloud:
    config:
      enabled: true
      # uri del config-server
      url: ${CONFIG_SERVER:http://localhost:8888}
      # uri: http://localhost:8888
  profiles:
    active: dev
