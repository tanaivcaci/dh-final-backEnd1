spring:
  application:
    name: eureka-server
  cloud:
    config:
      discovery:
        enabled: true
        service-id: server-config
      # uri del config-server
      # url: ${CONFIG_SERVER:http://localhost:8888}
  profiles:
    active: dev