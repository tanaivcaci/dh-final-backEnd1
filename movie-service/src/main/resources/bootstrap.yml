spring:
  application:
    name: movies-service
  cloud:
    config:
      enabled: true
      #uri del config server url: ${CONFIG_SERVER:http://localhost:8888}
      url: ${CONFIG_SERVER:http://localhost:8888}
  #perfil para que encuentre movies-service-dev.yml
  profiles:
    active: dev
