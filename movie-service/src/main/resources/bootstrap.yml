spring:
  application:
    name: movies-service
  cloud:
    config:
    	fail-fast: true
    	discovery:
		  enabled: true
		  service-id: server-config
		  #url: ${CONFIG_SERVER:http://localhost:8888}

  profiles:
    active: dev
